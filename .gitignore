node_modules/
.next/
.next-build-check/
.next-dev-codex/
.turbo/
coverage/
tsconfig.tsbuildinfo
.env
.env.*
!.env.example
dist/
build/
.vercel/
.DS_Store
npm-debug.log*
# Top-level working data only (journal, recorded tapes). Committed analysis
# artifacts live under public/data/ and must stay tracked.
/data/
scripts/railway_keepalive.py

# Python venv for the GPU/PyTorch training path (Jetson CUDA wheels)
.venv/
__pycache__/
*.pyc

# GPU trainer working data + run outputs (regenerable; large CSVs / logs).
# The exported candidate model (public/model/neural-edge-gpu.json) IS tracked.
scripts/gpu/data/
scripts/gpu/out/

.vercel

# Personal hackathon committee submissions / source renders (kept local only)
recursos/Respuestas_*
recursos/generar_*.py
recursos/render_*/
recursos/terminalvideo.mp4
